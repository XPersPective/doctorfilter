# PB-007 — Git geçmişindeki eski AdMob kimliği

## Status

BLOCKED

## Blocker

Gerçek AdMob uygulama kimliği `408dc4c` öncesi commit'lerin `AndroidManifest.xml`'inde okunabilir durumda. Temizlemek history rewrite + force-push gerektirir; yıkıcı/geri döndürülemez → sahip kararı gerekir.

## Needed Resolution

Sahip karar verir: (a) geçmişi yeniden yaz (BFG/git-filter-repo + force-push), (b) kimliği düşük riskli kabul edip bırak, (c) AdMob'da yeni kimlik alıp eskisini geçersizleştirmenin mümkün olup olmadığını araştır.

## Safe Work Remaining

Yok — üç seçenek de insan kararı/eylemi.

## Objective

C-020'nin ruhu: gerçek kimlik herkese açık geçmişte kalıcı olmasın ya da bilinçli şekilde kabul edilsin.

## Dependencies

None

## Affected Areas

- Git geçmişi (`408dc4c~1` ve öncesi)

## Acceptance Criteria

- SahiBin yazılı kararı §decisions'a ADR olarak işlenmiş; seçilen yol uygulanmış.

## Verification

Risk: HIGH

Required:
- Seçenek (a) ise: temizlik sonrası `git grep` geçmişte kimliği bulamıyor; açık PR/issue/klon etkisi değerlendirildi.

## Architecture Impact

Expected: NO
