# PB-004 — iOS derleme ve cihaz doğrulaması

## Status

BLOCKED

## Blocker

macOS + Xcode + Apple Developer hesabı gerekir; bu makinede yok, sahip tarafında da kurulu değil.

## Needed Resolution

Sahip bir Mac sağlar (veya derleme hizmeti kararı) + Apple Developer hesabı.

## Safe Work Remaining

Kod hazır: `ios/Runner/AppDelegate.swift`, `lib/core/math/ios_color_filter.dart`, `lib/presentation/screens/ios_setup_screen.dart`, `ios/DoctorFilterControl/` (henüz hedef olarak projeye ekli değil). Adımlar: `ios/DoctorFilterControl/README.md` ve eski plan `git show 5453c2b:MIMARI.md` §12.1 G1.

## Objective

iOS sürümünün derlenip cihazda/simülatörde doğrulanması: sistem parlaklığı kontrolü, Renk Filtreleri sihirbazı, Kısayollar entegrasyonu, iOS 18 Control.

## Dependencies

None

## Affected Areas

- `ios/**`
- `lib/presentation/screens/ios_setup_screen.dart`
- `lib/core/math/ios_color_filter.dart`

## Acceptance Criteria

- `flutter build ios` hatasız (cocoapods dahil).
- Renk Filtreleri sihirbazı adımları cihazda izleniyor.
- Kısayol oluşturma çalışıyor; Control eklenebiliyor (`DoctorFilterControl` hedefi projeye eklendi).
- Mağaza metni ADR-002'deki sınırları vaat etmiyor.

## Verification

Risk: HIGH

Required:
- Gerçek derleme + cihaz/simülatör turu
- `flutter analyze` temiz

## Architecture Impact

Expected: YES — `DoctorFilterControl` hedefi `ios/Runner.xcodeproj`e eklenecek.
