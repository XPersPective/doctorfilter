# PB-003 — Microsoft Store yayını

## Status

BLOCKED

## Blocker

Partner Center kaydı, Durable eklenti `doctorfilter_pro_lifetime` oluşturma ve gerçek `msix_config.publisher` değeri — hepsi sahibin Microsoft hesabında.

## Needed Resolution

Sahip Partner Center hesabı açar; uygulamayı kaydeder; Store kimliğinden `publisher` değerini iletir; eklentiyi tanımlar.

## Safe Work Remaining

Kod hazır: `windows/runner/store_purchases.cpp`, `lib/data/repositories/microsoft_store_purchase_repository.dart`; `flutter build windows` geçiyor. Yapılacak: `pubspec.yaml:msix_config.publisher` gerçek değer, MSIX imzalama, Store'a yükleme.

## Objective

Windows sürümünün MSIX olarak Microsoft Store'a yüklenebilir ve Store satın alma akışının çalışır olması (AC10 katkısı).

## Dependencies

None

## Affected Areas

- `pubspec.yaml` (`msix_config.publisher`)
- `windows/runner/store_purchases.cpp`

## Acceptance Criteria

- MSIX gerçek publisher ile paketleniyor.
- Store'da taslağa/gönderime hazır paket; satın alma test akışı geçiyor.

## Verification

Risk: MEDIUM

Required:
- `flutter build windows` + MSIX paketleme
- Partner Center onayı

## Architecture Impact

Expected: NO

## Decision Boundary

Fiyat/mağaza metni/yaş sınırı sahip kararı.
