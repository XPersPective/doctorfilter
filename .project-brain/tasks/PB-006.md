# PB-006 — Linux ve macOS derlemeleri

## Status

BLOCKED

## Blocker

Bu makinede yalnızca Windows var; Linux ve macOS ortamı gerekir (sahip temin edecek ya da CI'a taşınacak — sahip kararı).

## Needed Resolution

Sahip: derleme ortamı (fiziksel makine, VM veya CI runner) kararı.

## Safe Work Remaining

İskeletler repo'da (`linux/`, `macos/`). Overlay vaat edilmiyor (target.md Non-Goals). Yapılacak: `flutter build linux` / `flutter build macos` + açılış duman testi.

## Objective

İskeletlerin gerçekten derlendiğini ve uygulamanın overlaysız modda açıldığını doğrulamak.

## Dependencies

None

## Affected Areas

- `linux/**`
- `macos/**`

## Acceptance Criteria

- İki platformda da build başarılı.
- Uygulama açılıyor; overlay ilgili UI gizli/etiketsiz.

## Verification

Risk: LOW

Required:
- Platform build komutlarının çıktısı

## Architecture Impact

Expected: NO
