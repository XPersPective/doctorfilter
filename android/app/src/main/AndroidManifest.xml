<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions required for Screen Filter Overlay, Notifications and Scheduling -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Per-app exclusions: names the app in front so the filter can get out
         of the way for a camera or a gallery. Granted by the user in system
         settings, not at runtime, and the feature stays off until they do. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Android 14+: the filter is a user-set alarm clock in spirit; without an
         exact alarm the bedtime schedule drifts by minutes. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <!-- Google AdMob application ID. Filled in at build time from
             android/key.properties (admobAppId, not committed); a checkout
             without it builds with Google's official test ID. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Invisible trampoline for launcher shortcuts: starts the filter and
             finishes without drawing anything. -->
        <activity
            android:name=".ShortcutActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Modern Screen Overlay Service (Android 14+ specialUse compatible) -->
        <service
            android:name=".OverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Screen filter overlay to reduce blue light and eye strain" />
        </service>

        <!-- 20-20-20 eye break reminder -->
        <receiver
            android:name=".BreakReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Receiver for Schedule Timers and Boot Restore -->
        <receiver
            android:name=".ScheduleReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="com.crazypenguin.doctorfilter.ACTION_SCHEDULE_START" />
                <action android:name="com.crazypenguin.doctorfilter.ACTION_SCHEDULE_STOP" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings tile: toggle from the shade without opening the app -->
        <service
            android:name=".FilterTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Home-screen widget -->
        <receiver
            android:name=".FilterWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.crazypenguin.doctorfilter.widget.TOGGLE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_filter_info" />
        </receiver>

        <!-- Receiver for Quick Notification Bar Actions (Power, Dim, Next) -->
        <receiver
            android:name=".NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Apps with a launcher entry, which is exactly what the exclusion
             picker lists. Narrower than QUERY_ALL_PACKAGES on purpose. -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>
</manifest>
