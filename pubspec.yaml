name: doctorfilter
description: "DoctorFilter - blue light filter and eye strain reduction app with Kelvin-based screen warming, presets and scheduling."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# versionCode scheme: major*1000 + minor*100 + patch (must stay above legacy Play max 1015)
version: 2.0.0+2000

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.5.4
  sqflite: ^2.4.2
  path: ^1.9.1
  google_mobile_ads: ^9.1.0
  in_app_purchase: ^3.2.0
  app_tracking_transparency: ^2.0.6
  share_plus: ^12.0.0
  in_app_review: ^2.0.10
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.2
  path_provider: ^2.1.5
  file_picker: ^10.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Builds the Microsoft Store package from the Windows release build.
  # A dev dependency, not a runtime one: it is a packaging tool and has no
  # business shipping inside the app.
  msix: ^3.16.9

# Microsoft Store packaging. `dart run msix:create` turns the Windows release
# build into an installable .msix.
#
# Unsigned on purpose. Signing needs the certificate the Store issues against a
# real publisher identity, and a self-signed one would only teach testers to
# click through a warning. The Store re-signs the package on upload anyway, so
# the unsigned artefact is what you submit.
msix_config:
  display_name: DoctorFilter
  publisher_display_name: CrazyPenguin
  # Replaced by the Store with the identity from Partner Center on first
  # submission. Kept obviously-placeholder so it cannot be mistaken for real.
  identity_name: CrazyPenguin.DoctorFilter
  publisher: CN=PLACEHOLDER-REPLACE-WITH-PARTNER-CENTER-PUBLISHER-ID
  msix_version: 2.0.0.0
  logo_path: assets/images/icon.png
  # No capabilities beyond the default. The overlay is a plain layered window
  # and needs none; asking for more than is used is how a submission gets
  # questioned.
  capabilities: ''
  store: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/Localizations/

  # Orbitron only, for numeric readouts. The other four families shipped with
  # the 1.x design and nothing referenced them; a font nobody uses is bytes in
  # every download and one more licence to account for.
  fonts:
    - family: Orbitron
      fonts:
        - asset: assets/fonts/Orbitron-VariableFont_wght.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
